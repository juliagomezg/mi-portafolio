---
import Layout from "../layouts/Layout.astro"; // 📌 Importamos el layout

const lang = "es"; // 🔹 Cambia a "en" si quieres que la página esté en inglés

// Definimos las variables correctamente
const pageTitle = lang === "es" ? "Mi Portafolio" : "My Portfolio";
const greeting = lang === "es" ? "Hola, soy Julia Gómez👋" : "Hello, I'm Julia Gómez👋";
const description =
  lang === "es"
    ? "Desarrolladora Web en formación | React | Vue | TypeScript | Astro | FullStack | QA Testing"
    : "Web Developer in training | React | Vue | TypeScript | Astro | FullStack | QA Testing";
const projectsText = lang === "es" ? "Ver proyectos" : "See projects";
const contactText = lang === "es" ? "Contacto" : "Contact";
---

<Layout title={pageTitle} lang={lang}>  
  <main class="container mx-auto text-center p-10 bg-sky-950">
    <h1 class="text-4xl font-bold text-white">{greeting}</h1>
    <p class="text-lg mt-4 text-white">{description}</p>
    <nav class="mt-6">
      <a href="/projects" class="text-blue-400 hover:underline">{projectsText}</a> |
      <a href="/contact" class="text-blue-400 hover:underline">{contactText}</a>
    </nav>
  </main>  
</Layout>
